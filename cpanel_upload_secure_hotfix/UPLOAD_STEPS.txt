1. Open cPanel -> File Manager.
2. Go to the live secure API folder:
   public_html/apps/your-bridge-school/secure
   (if missing, try: public_html/app/secure)
3. Back up current files (right-click -> Copy):
   - .htaccess
   - bootstrap.php
   - upload_secure.php
   - push_secure.php
   - check_item_exists_secure.php
4. Upload these files from this folder into the secure API folder (overwrite):
   - .htaccess          (← FIXED: now passes X-Auth-Token header to PHP)
   - bootstrap.php      (← FIXED: CORS handling + secure_config fallback)
   - upload_secure.php  (← FIXED: 40+ allowed MIME types)
   - push_secure.php    (unchanged)
   - check_item_exists_secure.php (unchanged)
5. Test the upload from the website:
   - Open https://www.yourbridgeschool.com/
   - Log in as a student
   - Go to Mail -> compose -> attach a file (PNG, JPG, PDF, DOCX, MP3, etc.)
   - Send the message
6. If it still says "Missing bearer token", run these diagnostics:
   a) Preflight test (run in your terminal):
      curl -i -X OPTIONS "https://api.yourbridgeschool.com/apps/your-bridge-school/secure/upload_secure.php" \
        -H "Origin: https://www.yourbridgeschool.com" \
        -H "Access-Control-Request-Method: POST" \
        -H "Access-Control-Request-Headers: authorization,x-auth-token,x-auth-uid,x-requested-with"
      Expected: HTTP/1.1 204 No Content
   b) Header dump test (run in your terminal):
      curl -i "https://api.yourbridgeschool.com/apps/your-bridge-school/secure/upload_secure.php" \
        -H "Authorization: Bearer test123" \
        -H "X-Auth-Token: test456" \
        -H "X-Auth-Uid: testuser"
      Expected response: should return "Missing bearer token" or similar (not a CORS error)
   c) If both pass, the issue is in how the Flutter app sends the request; I will add more debugging.
